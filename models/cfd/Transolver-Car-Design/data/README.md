# Data Directory

Place the related dataset `OneScience/ShapeNetCar` here so the runtime can read:

```text
data/mlcfd_data
```

The model repository does not include dataset files. Download the dataset with:

```bash
modelscope download --dataset OneScience/ShapeNetCar --local_dir data
```
