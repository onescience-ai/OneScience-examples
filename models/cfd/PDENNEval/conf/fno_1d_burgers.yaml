fno_config:
  datapipe:
    source:
      data_dir: "${ONESCIENCE_PDENNEVAL_DATA_DIR}"
      file_name: "1D_Burgers_Sols_Nu0.001.hdf5"
    verbose: True
    data:
      single_file: True
      reduced_resolution: 4
      reduced_resolution_t: 5
      reduced_batch: 1
      initial_step: 10
      pde_name: "1D_Burgers"

    dataloader:
      batch_size: 64
      num_workers: 4
      pin_memory: False

  model:
    name: "FNO"
    num_channels: 1
    modes: 12
    width: 20

  training:
    output_dir: "./outputs/fno_1d_burgers/"
    if_training: True
    continue_training: False
    model_path: null
    save_period: 20
    seed: 0
    training_type: "autoregressive"
    t_train: 201
    unroll_step: 20
    epochs: 500
    
    optimizer:
      name: 'Adam'
      lr: 1.0e-3
      weight_decay: 1.0e-4
    
    scheduler:
      name: "StepLR"
      step_size: 100
      gamma: 0.5
